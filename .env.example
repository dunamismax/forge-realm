# Hugo build configuration
HUGO_BASEURL=https://forge-realm.com
HUGO_ENV=production

# Cloudflare Pages configuration (non-secret)
# Keep the API token in GitHub Secrets, not here.
CLOUDFLARE_PAGES_PROJECT=forge-realm
CLOUDFLARE_ACCOUNT_ID=

# Optional: override Hugo version used by CI (defaults in workflow)
# HUGO_VERSION=0.146.0

